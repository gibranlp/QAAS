#!/usr/bin/env bash
# Funcion para cambiar el wallpaper aleatoriamente
function rand_wall (){
  dir='/home/gibranlp/Cloud/wallPapers/'
  wal --backend haishoku -a 85 -i "${dir[RANDOM % ${#dir[@]}]}" -q -l  &
}

rand_wall &
qtile-cmd -o cmd -f restart &
